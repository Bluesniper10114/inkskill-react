mutation UpdateProfile($data: ProfileFormInput!) {
  updateProfile(data: $data) {
    _id
    username
    name
    bio
    location {
      gid
      city
      state
      country
    }
    urls {
      web
    }
  }
}
