import React from 'react';
import Page from 'core/components/PublicPage';

const Index = ({ params }) => (
  <Page>
    <h1>Detail Page Placeholder #{params.id}</h1>
  </Page>
);

export default Index;
